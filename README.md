# datlechin's Homebrew tap

```sh
brew install datlechin/tap/pitboard        # the command line
brew install --cask datlechin/tap/pitboard # the menu bar app
```

[pitboard](https://github.com/datlechin/pitboard) switches between your own Claude Code
logins and shows how much each one has left.
